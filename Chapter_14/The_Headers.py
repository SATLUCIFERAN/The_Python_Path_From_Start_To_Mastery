
# A sample Python dictionary that represents HTTP headers

headers = {
    # Your mission clearance. You are a 'Bearer' of this token.
    "Authorization": "Bearer JEDICLASS-456-ABCDEF",
    # We are sending data formatted as JSON.
    "Content-Type": "application/json",
    # We will only accept a response formatted as JSON.
    "Accept": "application/json",
    # Your user agent identifies your ship to the server.
    "User-Agent": "Rebel-Alliance-Scraper-1.0"
}